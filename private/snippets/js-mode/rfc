# -*- mode: snippet -*-
#name : ReactFunctionalComponent
#key : rfc
#contributor: Christopher Olsson
# --
import React from 'react';

const ${1:name} = (${2:props}) => {
  return ($0);
};

export default $1;